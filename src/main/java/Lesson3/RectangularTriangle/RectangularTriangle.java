package Lesson3.RectangularTriangle;

public class RectangularTriangle {
    public static boolean rectangularTriangle(int a, int b, int c) {
        return Math.pow(a, 2) + Math.pow(b, 2) == Math.pow(c, 2);
    }
}
